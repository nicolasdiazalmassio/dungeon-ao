    <nav class="navbar navbar-expand-lg navbar-black bg-black" id= "navigation">
      <div class="container">
        <%= link_to "Dungeon AO", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          </ul>
          <ul class="navbar-nav ml-auto">
            <% if usuario_signed_in? %>
              <% if logeado? %>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
                    <%= "#{current_usuario.email}(#{personaje_actual.nombre})" %>
                  </a>
                  <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDropdown">
                    <%= link_to "Editar perfil", edit_usuario_registration_path(current_usuario), class: "dropdown-item" %>
                    <%= link_to "Cambiar de personaje", salir_session_personaje_path(personaje_actual), class: "dropdown-item text-danger", method: :delete, data: { confirm: "¿Estás seguro?" } %> 
                    <%= link_to "Cerrar sesion", destroy_usuario_session_path, method: :delete, class: "dropdown-item" %>
                  </ul>
                </li>
              <% else %>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
                    <%= current_usuario.email %>
                  </a>
                  <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDropdown">
                    <%= link_to "Editar perfil", edit_usuario_registration_path(current_usuario), class: "dropdown-item" %>
                    <%= link_to "Cerrar sesion", destroy_usuario_session_path, method: :delete, class: "dropdown-item" %>
                  </ul>
                </li>
              <% end %>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>







